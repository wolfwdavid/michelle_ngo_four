---
phase: 05-pbs-american-portrait
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/lib/components/TopNav.svelte
  - src/lib/components/TopNav.test.ts
  - src/routes/pbs-american-portrait/+page.svelte
  - src/routes/pbs-american-portrait/+page.ts
  - src/routes/pbs-american-portrait/_pbsCollectionUrl.ts
  - src/routes/pbs-american-portrait/_pbsCollectionUrl.test.ts
  - src/routes/pbs-american-portrait/page.test.ts
  - src/routes/watch/[id]/+page.svelte
  - src/routes/watch/[id]/page.test.ts
  - src/routes/work/[category]/+page.svelte
  - src/routes/work/[category]/page.test.ts
autonomous: true
requirements: [PBS-01, PBS-02, PBS-03]
nyquist_compliant: true

must_haves:
  truths:
    - "User navigates to /pbs-american-portrait/ from the TopNav PBS link (D-02) and sees the landing page (PBS-01)"
    - "Landing page shows h1 in PBS accent color, subtitle line, verbatim PBS blockquote with attribution, outbound link to pbs.org/american-portrait (D-08, D-09, D-10, D-12)"
    - "Landing page shows h2 'Stories' + 2/3/4 responsive grid of all 18 PBS videos in featured-first/date-desc order (D-16, D-18, D-19, D-20, PBS-02)"
    - "15 of 18 cards show a 'See on PBS →' badge linking to the per-video PBS collection URL; 3 cards (no collection URL) omit the badge (D-21, PBS-02)"
    - "TopNav PBS link is highlighted in cat-pbs accent color on BOTH /pbs-american-portrait/ AND /work/pbs-american-portrait/ (D-03)"
    - "/work/pbs-american-portrait/ still prerenders, still shows 18 cards, with a new inline cross-link to /pbs-american-portrait/ above the grid (D-04, D-06, PBS-03)"
    - "/watch/[id] PBS-category videos render an inline cross-link to /pbs-american-portrait/ below the description; non-PBS videos do not (D-05)"
    - "All previously-skipped Phase 5 tests flip to passing; full test suite + build + prerender script all exit 0"
  artifacts:
    - path: "src/routes/pbs-american-portrait/+page.svelte"
      provides: "Full PBS landing page — h1 + subtitle + blockquote + outbound + h2 + grid + per-card badges"
      contains: "PBS American Portrait"
      min_lines: 40
    - path: "src/routes/pbs-american-portrait/+page.ts"
      provides: "Real load() — getByCategory('PBS American Portrait') + D-18 sort"
      contains: "getByCategory"
    - path: "src/routes/pbs-american-portrait/_pbsCollectionUrl.ts"
      provides: "Real regex extractor (replaces 05-01 stub)"
      contains: "pbs.org/american-portrait/collection"
    - path: "src/lib/components/TopNav.svelte"
      provides: "Special-cased PBS href + extended isActive() predicate"
      contains: "pbs-american-portrait"
    - path: "src/routes/watch/[id]/+page.svelte"
      provides: "Conditional D-05 cross-link below description for PBS videos"
      contains: "About the PBS American Portrait project"
    - path: "src/routes/work/[category]/+page.svelte"
      provides: "Conditional D-04 cross-link after h1 for PBS category"
      contains: "About the PBS American Portrait project"
    - path: "build/pbs-american-portrait/index.html"
      provides: "Prerendered landing route (proves PBS-01 reachability)"
    - path: "build/work/pbs-american-portrait/index.html"
      provides: "Existing prerendered filter route (proves PBS-03 parity preserved)"
  key_links:
    - from: "src/lib/components/TopNav.svelte"
      to: "src/routes/pbs-american-portrait/+page.svelte"
      via: "${base}/pbs-american-portrait/ href on the PBS-only nav link"
      pattern: "pbs-american-portrait"
    - from: "src/routes/pbs-american-portrait/+page.svelte"
      to: "src/lib/data/index.ts"
      via: "getByCategory('PBS American Portrait') in +page.ts load()"
      pattern: "getByCategory"
    - from: "src/routes/pbs-american-portrait/+page.svelte"
      to: "src/routes/pbs-american-portrait/_pbsCollectionUrl.ts"
      via: "import { pbsCollectionUrl } + conditional <a> in grid <li>"
      pattern: "pbsCollectionUrl"
    - from: "src/routes/watch/[id]/+page.svelte"
      to: "src/routes/pbs-american-portrait/+page.svelte"
      via: "conditional <a href={`${base}/pbs-american-portrait/`}> below description when video.category === 'PBS American Portrait'"
      pattern: "pbs-american-portrait"
    - from: "src/routes/work/[category]/+page.svelte"
      to: "src/routes/pbs-american-portrait/+page.svelte"
      via: "conditional <a href={`${base}/pbs-american-portrait/`}> after h1 when data.category === 'PBS American Portrait'"
      pattern: "pbs-american-portrait"
---

<objective>
Build out the full Phase 5 vertical slice on top of Plan 05-01's Wave 0 scaffolding:

  1. Real PBS landing page at /pbs-american-portrait/ — load + render the 18 PBS videos as a 2/3/4 grid below a context block (h1 + subtitle + verbatim blockquote + outbound link + h2 "Stories"). 15 of 18 cards get a per-card "See on PBS →" badge linked to each video's pbs.org/american-portrait/collection/... URL.
  2. TopNav extension — retarget the PBS category link to /pbs-american-portrait/ and extend isActive() so the link highlights on BOTH /pbs-american-portrait/ AND /work/pbs-american-portrait/.
  3. Two cross-links — D-04 on /work/pbs-american-portrait/ (after h1) and D-05 on /watch/[id] (below description, PBS videos only), both pointing to /pbs-american-portrait/.
  4. Flip every Plan 05-01 describe.skip block to describe — Wave 0 → Wave 1 one-rule transition.

Purpose: ship PBS-01, PBS-02, PBS-03 in one wave by consuming the test contract Plan 05-01 wrote.

Output: 4 routes/components fully implemented + ~24 previously-skipped assertions flipped to GREEN.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-pbs-american-portrait/05-CONTEXT.md
@.planning/phases/05-pbs-american-portrait/05-RESEARCH.md
@.planning/phases/05-pbs-american-portrait/05-VALIDATION.md
@.planning/phases/05-pbs-american-portrait/05-01-PLAN.md
@.planning/phases/05-pbs-american-portrait/05-01-SUMMARY.md

# Phase 3 contracts (carry-forward — reuse markup + sort + categoryAccent verbatim)
@src/routes/work/[category]/+page.svelte
@src/routes/work/[category]/+page.ts
@src/routes/watch/[id]/+page.svelte
@src/lib/components/TopNav.svelte
@src/lib/components/categoryAccent.ts

<interfaces>
<!--
  Locked APIs the executor uses directly (no exploration needed).

  CRITICAL: Read the <approved>...</approved> block inside 05-01-PLAN.md before
  starting Task 2. That paragraph is the verbatim PBS copy you embed literally
  into +page.svelte's <blockquote>. Do NOT re-fetch, do NOT paraphrase, do NOT
  re-decide. If <approved> is absent or empty, halt and ask the user.
-->

From src/lib/data/index.ts:
```typescript
export type { Video, Category } from './...';
export { getByCategory, videos, producerReelId, categoryToSlug } from './...';
// getByCategory(category: Category): readonly Video[]
// Each Video has: { id: string; category: Category; description: string; featured: boolean; published: string; ... }
```

From src/lib/components/categoryAccent.ts:
```typescript
export function categoryAccent(category: Category): string;
// categoryAccent('PBS American Portrait') === 'text-cat-pbs'  ← THE ACTUAL CLASS (NOT text-cat-pbs-american-portrait — see RESEARCH Pitfall 7)
```

D-18 / D-25 sort comparator (verbatim from src/routes/work/[category]/+page.ts):
```typescript
[...getByCategory('PBS American Portrait')].toSorted((a, b) => {
  if (a.featured !== b.featured) return a.featured ? -1 : 1;
  return b.published.localeCompare(a.published);
})
```

base path import (every internal link wraps through this):
```typescript
import { base } from '$app/paths';
// Use: href={`${base}/pbs-american-portrait/`}  (note trailing slash — trailingSlash='always')
```

ESLint disable line (use ONLY in files that build internal hrefs from base + string template — this is the established Phase 3 project idiom, see VideoCard.svelte, TopNav.svelte, /watch/[id]/+page.svelte):
```svelte
<script lang="ts">
  /* eslint-disable svelte/no-navigation-without-resolve */
  // ...
</script>
```

Stub locations from Plan 05-01 (you REPLACE these — they currently contain only placeholder code):
  - src/routes/pbs-american-portrait/+page.ts            — stub returns { videos: [], pbsBlurb: '' }
  - src/routes/pbs-american-portrait/+page.svelte       — stub renders a single <p>
  - src/routes/pbs-american-portrait/_pbsCollectionUrl.ts — stub returns null

Existing TopNav isActive() (src/lib/components/TopNav.svelte lines 100-113):
```typescript
function isActive(slug: string): boolean {
  return page.url.pathname.replace(/\/$/, '').endsWith(`/work/${slug}`);
}
```

Existing TopNav PBS link rendering (src/lib/components/TopNav.svelte lines 122-133):
```svelte
{#each categories as category (category)}
  {@const slug = categoryToSlug(category)}
  <li>
    <a
      href={`${base}/work/${slug}`}
      data-sveltekit-preload-data="hover"
      class={isActive(slug) ? categoryAccent(category) : 'text-neutral-300 hover:text-white'}
    >
      {category}
    </a>
  </li>
{/each}
```

PBS audit data (RESEARCH §Code Examples 5 — verified at plan time):
  - 18 PBS records total
  - 15 have a `https://www.pbs.org/american-portrait/collection/...` URL in `description`
  - 3 do NOT: id `620232398` (Year in Review 2020), id `1007061884` (Celebrity Message), id `1007027015` (Introducing PBS)
  - Post-D-18-sort first card: id 1023002503 (Hispanic Heritage Month, featured + 2024-10-24)

Verbatim approved PBS copy:
  - Lives in `<approved>...</approved>` inside `.planning/phases/05-pbs-american-portrait/05-01-PLAN.md`
  - You embed it as inline text between `<blockquote>` tags in src/routes/pbs-american-portrait/+page.svelte (NOT via load() return, NOT via a strings file)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend TopNav.svelte — D-02 PBS link retarget + D-03 isActive() extension</name>
  <files>
    src/lib/components/TopNav.svelte,
    src/lib/components/TopNav.test.ts
  </files>
  <read_first>
    - src/lib/components/TopNav.svelte (full file — see <interfaces> for the two key blocks)
    - src/lib/components/TopNav.test.ts (existing tests — line 78 has the PBS-specific href assertion that needs updating)
    - .planning/phases/05-pbs-american-portrait/05-CONTEXT.md (D-02, D-03 — exact contract)
    - .planning/phases/05-pbs-american-portrait/05-RESEARCH.md §Pattern 5 (two-branch predicate shape) §Pitfall 1 (slug guard) §Pitfall 8 (endsWith with trailing-slash strip — keep using this pattern)
  </read_first>
  <behavior>
    After this task:
    - PBS category nav `<a>` href === `${base}/pbs-american-portrait/` (NOTE: trailing slash — trailingSlash='always' demands it).
    - All 7 other category nav `<a>` hrefs unchanged: `${base}/work/${slug}` (no trailing slash on these — the existing pattern produces `/work/<slug>` without one because the existing test asserts `'/work/pbs-american-portrait'` for the OLD PBS check that we're updating away from; same trailing-slash-omission pattern stays for the other 7).
    - `isActive(slug)` returns true on BOTH `/pbs-american-portrait/` AND `/work/pbs-american-portrait/` when called with slug `'pbs-american-portrait'`.
    - `isActive('reel')` (or any other non-PBS slug) on `/pbs-american-portrait/` returns FALSE.
    - All 5 NEW Phase 5 TopNav test assertions (from Plan 05-01 Task 3 EDIT 1) flip from describe.skip → describe and PASS.
    - The existing `it('category links use /work/<slug> hrefs', ...)` test's PBS-specific assertion (line 78) is updated from `'/work/pbs-american-portrait'` → `'/pbs-american-portrait'`.
    - The existing `describe('TopNav — active state (D-41)', ...)` PBS tests STILL pass (they assert text-cat-pbs class on /work/pbs-american-portrait + trailing-slash variant — both still match per D-03 extension).
  </behavior>
  <action>
    --- EDIT 1: src/lib/components/TopNav.svelte ---

    A) Replace the existing `href={`${base}/work/${slug}`}` template on the category `<a>` with a PBS-special-cased href. Find the existing block (around lines 122-132):
    ```svelte
    {#each categories as category (category)}
      {@const slug = categoryToSlug(category)}
      <li>
        <a
          href={`${base}/work/${slug}`}
          data-sveltekit-preload-data="hover"
          class={isActive(slug) ? categoryAccent(category) : 'text-neutral-300 hover:text-white'}
        >
          {category}
        </a>
      </li>
    {/each}
    ```

    Replace with:
    ```svelte
    {#each categories as category (category)}
      {@const slug = categoryToSlug(category)}
      {@const href = slug === 'pbs-american-portrait' ? `${base}/pbs-american-portrait/` : `${base}/work/${slug}`}
      <li>
        <a
          {href}
          data-sveltekit-preload-data="hover"
          class={isActive(slug) ? categoryAccent(category) : 'text-neutral-300 hover:text-white'}
        >
          {category}
        </a>
      </li>
    {/each}
    ```

    Why ternary literal (not conditional rendering): keeps Tailwind v4 scanner happy (no class concat) and matches the Phase 4 D-13 navClass two-literals pattern.

    B) Extend `isActive(slug)` (lines 100-113). REPLACE the function body:
    ```typescript
    function isActive(slug: string): boolean {
      // Normalize trailing slash, then suffix-match the slug. See file header
      // comment for the two production forces this normalization handles
      // (trailingSlash='always' + adapter-static relative `base`).
      const normalized = page.url.pathname.replace(/\/$/, '');
      // Phase 3 D-41: standard /work/<slug> active match.
      if (normalized.endsWith(`/work/${slug}`)) return true;
      // Phase 5 D-03: PBS gets a flagship landing surface that also counts as "active".
      // Slug guard prevents future categories with the same suffix from false-positive.
      if (slug === 'pbs-american-portrait' && normalized.endsWith('/pbs-american-portrait')) {
        return true;
      }
      return false;
    }
    ```

    --- EDIT 2: src/lib/components/TopNav.test.ts ---

    A) FLIP the new Phase 5 describe block (added in Plan 05-01 Task 3 EDIT 1) from `describe.skip` → `describe`. One-rule string replace on the line containing `describe.skip('TopNav — Phase 5 PBS retarget + active-state extension'`.

    B) UPDATE the existing Phase 3 test assertion on line ~78 (inside `describe('TopNav — NAV-01 baseline rendering (D-39, D-40)', ...)` → `it('category links use /work/<slug> hrefs', ...)`):

    Find:
    ```typescript
    expect(pbsLink?.getAttribute('href')).toBe('/work/pbs-american-portrait');
    ```

    Replace with:
    ```typescript
    // Phase 5 D-02: PBS retargeted to /pbs-american-portrait (special-cased).
    // The 7 other category links still use /work/<slug>.
    expect(pbsLink?.getAttribute('href')).toBe('/pbs-american-portrait/');
    ```

    ALSO update the test's title and intro narration if you want (optional cosmetic): the test name `category links use /work/<slug> hrefs` is now technically untrue for PBS — recommended rename: `'PBS link uses /pbs-american-portrait/ href (D-02); other 7 categories use /work/<slug>'`. Add a follow-up assertion in the same `it()` to lock the other 7:
    ```typescript
    const reelLink = Array.from(host.querySelectorAll('a')).find(
      (a) => a.textContent?.trim() === 'Reel'
    );
    expect(reelLink?.getAttribute('href')).toBe('/work/reel');
    ```

    --- VALIDATION ---
    ```
    pnpm vitest run src/lib/components/TopNav.test.ts
    pnpm check
    pnpm lint src/lib/components/TopNav.svelte src/lib/components/TopNav.test.ts
    ```

    All MUST exit 0. The Phase 5 describe block (now unskipped) has 5 it() blocks, all should pass.
  </action>
  <verify>
    <automated>pnpm vitest run src/lib/components/TopNav.test.ts --reporter=basic &amp;&amp; pnpm check 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "pbs-american-portrait" src/lib/components/TopNav.svelte` returns at least 2 matches (one in the href ternary, one in the isActive guard).
    - `grep -F "describe.skip" src/lib/components/TopNav.test.ts` does NOT include the line containing `'TopNav — Phase 5 PBS retarget + active-state extension'` (the .skip was removed).
    - `grep -F "/pbs-american-portrait/" src/lib/components/TopNav.test.ts` returns at least 2 matches (the updated href assertion on line ~78 + the new D-03 test assertions).
    - `pnpm vitest run src/lib/components/TopNav.test.ts` exits 0 — all assertions (existing + new Phase 5 block) pass.
    - `pnpm check` exits 0.
    - `pnpm lint` exits 0 on TopNav.svelte + TopNav.test.ts (the `/* eslint-disable svelte/no-navigation-without-resolve */` directive already present at line 30 covers the new href ternary).
    - No new ESLint disables added beyond the existing file-level directive.
  </acceptance_criteria>
  <done>TopNav.svelte renders PBS link with `/pbs-american-portrait/` href; isActive matches both surfaces; all TopNav tests (Phase 3 baseline + Phase 4 scroll-aware + Phase 5 new block) green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build /pbs-american-portrait/ landing page — load + helper + full layout + verbatim copy</name>
  <files>
    src/routes/pbs-american-portrait/+page.ts,
    src/routes/pbs-american-portrait/+page.svelte,
    src/routes/pbs-american-portrait/_pbsCollectionUrl.ts,
    src/routes/pbs-american-portrait/_pbsCollectionUrl.test.ts,
    src/routes/pbs-american-portrait/page.test.ts
  </files>
  <read_first>
    - .planning/phases/05-pbs-american-portrait/05-01-PLAN.md (READ the `<approved>...</approved>` block inside `<approved_pbs_copy>` — verbatim copy you embed in <blockquote>)
    - .planning/phases/05-pbs-american-portrait/05-CONTEXT.md (D-08..D-22 — full page composition)
    - .planning/phases/05-pbs-american-portrait/05-RESEARCH.md §Code Examples 1-3 (literal shapes) §Pitfalls 3, 4, 6, 7
    - src/routes/work/[category]/+page.svelte (mirror this grid markup verbatim for D-19)
    - src/routes/work/[category]/+page.ts (D-25 sort comparator verbatim — copy lines 32-35)
    - src/routes/+page.ts (sync `PageLoad` shape for parameterless prerendered route)
    - src/lib/components/VideoCard.svelte (component contract — `{video, eager}` props; do NOT modify)
    - src/lib/components/categoryAccent.ts (`text-cat-pbs` is the LITERAL class — Pitfall 7)
    - src/routes/pbs-american-portrait/+page.ts (stub from Plan 05-01 — you OVERWRITE)
    - src/routes/pbs-american-portrait/+page.svelte (stub — OVERWRITE)
    - src/routes/pbs-american-portrait/_pbsCollectionUrl.ts (stub — OVERWRITE with real regex)
    - src/routes/pbs-american-portrait/_pbsCollectionUrl.test.ts (Plan 05-01 stubs — flip .skip → describe)
    - src/routes/pbs-american-portrait/page.test.ts (Plan 05-01 stubs — flip .skip → describe)
  </read_first>
  <behavior>
    After this task, every Plan 05-01 describe.skip in `_pbsCollectionUrl.test.ts` (3 blocks) and `page.test.ts` (3 blocks — load, render, badges) flips to GREEN.

    Helper (`pbsCollectionUrl`):
    - Regex `/https?:\/\/(?:www\.)?pbs\.org\/american-portrait\/collection\/[^\s)]+/` extracts the first URL.
    - Trailing `[).,!?]+` stripped (defensive — RESEARCH Pitfall 3).
    - Returns `null` when no match.

    Load:
    - Returns `{ videos }` — 18 PBS Video records, D-18 sort applied.
    - Sync (no async — no error() throw).
    - No `entries()` — parameterless route.
    - No `pbsBlurb` in return (the copy is inlined verbatim in +page.svelte — D-11).

    Render — top-to-bottom (D-14):
    1. `<h1>PBS American Portrait</h1>` with class containing `text-cat-pbs` (D-08).
    2. `<p>` subtitle text verbatim `18 stories produced by Michelle Ngo` with classes `text-sm md:text-base tracking-wide text-neutral-400 uppercase` (D-09).
    3. `<blockquote>` containing the user-approved verbatim PBS paragraph (read from `<approved>...</approved>` in 05-01-PLAN.md); styling `border-l-2 border-neutral-700 pl-4` (D-10).
    4. Attribution `<p>` below blockquote: `Description from pbs.org/american-portrait` in `text-xs text-neutral-500` (D-10).
    5. `<p><a>Visit pbs.org/american-portrait →</a></p>` outbound link, `target="_blank" rel="noopener"`, hover-underline (D-12, Pitfall 6).
    6. `<h2>Stories</h2>` (D-16).
    7. `<ul class="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-2 sm:gap-3">` containing 18 `<li>` items, each with `<VideoCard {video} eager={i < 8} />` followed by a conditional `{#if pbsCollectionUrl(video.description)} <a> See on PBS → </a> {/if}` (D-19, D-20, D-21).
    8. Container `<section class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-8">` (D-15).
    9. `<svelte:head><title>Michelle Ngo — PBS American Portrait</title></svelte:head>`.
  </behavior>
  <action>
    --- FILE 1: REPLACE src/routes/pbs-american-portrait/_pbsCollectionUrl.ts ENTIRELY ---
    ```ts
    /**
     * Phase 5 D-21: extracts the first pbs.org/american-portrait/collection/... URL
     * from a video's description. Returns null if no match.
     *
     * Underscore prefix excludes this file from SvelteKit route detection
     * (SvelteKit 2.59.1 ignores `_*` files under src/routes/*).
     *
     * Defensive trailing-punctuation strip: today's 18 PBS descriptions have no
     * trailing punctuation on the URL (audit-verified at plan time), but a future
     * copy edit could introduce a `.` or `,` — this trim is a free safety net.
     *
     * Verified regex `[^\s)]+` (negate space AND closing paren) handles the
     * inline-parenthesis case from RESEARCH Pitfall 3.
     */
    const COLLECTION_URL =
      /https?:\/\/(?:www\.)?pbs\.org\/american-portrait\/collection\/[^\s)]+/;
    const TRAILING_PUNCT = /[).,!?]+$/;

    export function pbsCollectionUrl(description: string): string | null {
      const m = description.match(COLLECTION_URL);
      if (!m) return null;
      return m[0].replace(TRAILING_PUNCT, '');
    }
    ```

    --- FILE 2: REPLACE src/routes/pbs-american-portrait/+page.ts ENTIRELY ---
    ```ts
    /**
     * /pbs-american-portrait/ — Phase 5 D-01 flagship landing route.
     *
     * Parameterless prerendered route. `prerender = true` + `trailingSlash = 'always'`
     * inherited from src/routes/+layout.ts. No entries() needed (flat route).
     *
     * Load returns the 18 PBS videos sorted D-18 (featured-first then date-desc),
     * mirroring /work/[category]/+page.ts shape.
     *
     * Sync (no async) — no error() throw, no rejection contract; matches Phase 4 /+page.ts.
     */
    import type { PageLoad } from './$types';
    import { getByCategory } from '$lib/data';

    export const load: PageLoad = () => ({
      videos: [...getByCategory('PBS American Portrait')].toSorted((a, b) => {
        if (a.featured !== b.featured) return a.featured ? -1 : 1;
        return b.published.localeCompare(a.published);
      }),
    });
    ```

    --- FILE 3: REPLACE src/routes/pbs-american-portrait/+page.svelte ENTIRELY ---

    CRITICAL: Before writing this file, READ `<approved>...</approved>` from
    `.planning/phases/05-pbs-american-portrait/05-01-PLAN.md`. The verbatim paragraph
    inside that tag is what you embed between `<blockquote>` and `</blockquote>` below.
    Do NOT paraphrase. Do NOT add quotation marks. Do NOT split into multiple lines.

    ```svelte
    <!--
      /pbs-american-portrait/ — Phase 5 flagship landing page.

      Decisions implemented:
        D-08 — h1 in PBS accent color (text-cat-pbs via categoryAccent helper)
        D-09 — subtitle "18 stories produced by Michelle Ngo" (portfolio-forward credit)
        D-10 — verbatim PBS blockquote + attribution
        D-12 — outbound link to pbs.org/american-portrait (target=_blank rel=noopener)
        D-14 — editorial minimal DOM order: h1 / subtitle / blockquote / outbound / h2 / grid
        D-15 — container max-w-7xl px-4 sm:px-6 lg:px-8 (matches Phase 3 D-23)
        D-16 — h2 "Stories" (no count — implied by visible grid)
        D-17 — TopNav stays solid (no scroll-aware on this route — Phase 4 D-13 contract preserved)
        D-18 — featured-first then published-desc sort (D-25 inherited)
        D-19 — grid markup mirrors /work/[category] verbatim
        D-20 — VideoCard reused verbatim, eager={i < 8}
        D-21 — per-card "See on PBS →" badge in parent <li> (NOT in VideoCard)

      ESLint: svelte/no-navigation-without-resolve disabled (project idiom — internal
      hrefs built from $app/paths base; same pattern as VideoCard + TopNav + /watch/[id]).
    -->
    <script lang="ts">
      /* eslint-disable svelte/no-navigation-without-resolve */
      import type { PageData } from './$types';
      import VideoCard from '$lib/components/VideoCard.svelte';
      import { categoryAccent } from '$lib/components/categoryAccent';
      import { pbsCollectionUrl } from './_pbsCollectionUrl';

      let { data }: { data: PageData } = $props();
    </script>

    <svelte:head>
      <title>Michelle Ngo — PBS American Portrait</title>
    </svelte:head>

    <section class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-8">
      <h1
        class="text-3xl md:text-5xl font-bold uppercase tracking-wider {categoryAccent(
          'PBS American Portrait',
        )}"
      >
        PBS American Portrait
      </h1>
      <p class="mt-3 text-sm md:text-base tracking-wide text-neutral-400 uppercase">
        18 stories produced by Michelle Ngo
      </p>

      <blockquote class="mt-6 border-l-2 border-neutral-700 pl-4 text-neutral-200 max-w-3xl">
        {APPROVED_PBS_PARAGRAPH_VERBATIM_FROM_05-01-PLAN.md}
      </blockquote>
      <p class="mt-2 text-xs text-neutral-500">Description from pbs.org/american-portrait</p>

      <p class="mt-6">
        <a
          href="https://www.pbs.org/american-portrait/"
          target="_blank"
          rel="noopener"
          class="hover:underline"
        >
          Visit pbs.org/american-portrait →
        </a>
      </p>

      <h2 class="mt-12 text-lg font-medium">Stories</h2>
      <ul class="mt-4 grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-2 sm:gap-3">
        {#each data.videos as video, i (video.id)}
          <li>
            <VideoCard {video} eager={i < 8} />
            {#if pbsCollectionUrl(video.description)}
              <a
                href={pbsCollectionUrl(video.description) ?? ''}
                target="_blank"
                rel="noopener"
                class="mt-1 inline-block text-xs text-neutral-400 hover:underline"
              >
                See on PBS →
              </a>
            {/if}
          </li>
        {/each}
      </ul>
    </section>
    ```

    REPLACE the literal token `{APPROVED_PBS_PARAGRAPH_VERBATIM_FROM_05-01-PLAN.md}` (including curly braces and the dotted path) with the verbatim text from `<approved>...</approved>` inside `.planning/phases/05-pbs-american-portrait/05-01-PLAN.md`. Render it as PLAIN TEXT between `<blockquote>` and `</blockquote>` (no surrounding `{...}` interpolation — Svelte template literals would interpret braces; this is verbatim copy as static markup).

    Example (for Candidate C):
    ```svelte
    <blockquote class="mt-6 border-l-2 border-neutral-700 pl-4 text-neutral-200 max-w-3xl">
      Whether it's joy or sorrow, triumph or hardship, family traditions followed for decades or just the chaos of the morning school run, PBS American Portrait put together a picture of life as it's really lived. The show gives a glimpse into American life, and a chance for everyday Americans to be heard.
    </blockquote>
    ```

    HALT and ask the user if `<approved>...</approved>` is missing or empty.

    --- FILE 4: src/routes/pbs-american-portrait/_pbsCollectionUrl.test.ts — flip .skip ---

    Three describe.skip blocks → describe (one-rule rename per block):
      - `describe.skip('pbsCollectionUrl — 15 positive extractions (D-21)'`  → `describe('pbsCollectionUrl — 15 positive extractions (D-21)'`
      - `describe.skip('pbsCollectionUrl — returns null for 3 records without URL (Pitfall 4)'` → `describe('pbsCollectionUrl — returns null for 3 records without URL (Pitfall 4)'`
      - `describe.skip('pbsCollectionUrl — edge cases'` → `describe('pbsCollectionUrl — edge cases'`

    No other changes.

    --- FILE 5: src/routes/pbs-american-portrait/page.test.ts — flip .skip ---

    Three describe.skip blocks → describe (one-rule rename per block):
      - `describe.skip('/pbs-american-portrait load — PBS-02 (D-18 sort, 18 cards)'` → `describe(...)`
      - `describe.skip('/pbs-american-portrait render — PBS-02 (D-08, D-09, D-10, D-12, D-16, D-19)'` → `describe(...)`
      - `describe.skip('/pbs-american-portrait per-card PBS badges — PBS-02 (D-21)'` → `describe(...)`

    No other changes.

    --- VALIDATION ---
    ```
    pnpm vitest run src/routes/pbs-american-portrait/ --reporter=basic
    pnpm check
    pnpm lint src/routes/pbs-american-portrait/
    pnpm build
    pnpm test:prerender
    ```

    All MUST exit 0.

    PARTICULARLY: confirm the helper test asserts on 15 positive extractions correctly. If a record's description regex match exceeds `[^\s)]+` boundaries (e.g. URL embedded in a description that contains stray bytes), the positive-extraction loop will fail. The audit (RESEARCH §Code Examples 5) confirms 15/15 today.
  </action>
  <verify>
    <automated>pnpm vitest run src/routes/pbs-american-portrait/ --reporter=basic &amp;&amp; pnpm check 2>&amp;1 | tail -10 &amp;&amp; pnpm build 2>&amp;1 | tail -10 &amp;&amp; pnpm test:prerender</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "pbs.org/american-portrait/collection" src/routes/pbs-american-portrait/_pbsCollectionUrl.ts` returns at least 1 match.
    - `grep -F "describe.skip" src/routes/pbs-american-portrait/_pbsCollectionUrl.test.ts` returns 0 matches.
    - `grep -F "describe.skip" src/routes/pbs-american-portrait/page.test.ts` returns 0 matches.
    - `grep -F "getByCategory('PBS American Portrait')" src/routes/pbs-american-portrait/+page.ts` returns at least 1 match.
    - `grep -F "categoryAccent(" src/routes/pbs-american-portrait/+page.svelte` returns at least 1 match.
    - `grep -F "<blockquote" src/routes/pbs-american-portrait/+page.svelte` returns at least 1 match.
    - `grep -F "Description from pbs.org/american-portrait" src/routes/pbs-american-portrait/+page.svelte` returns at least 1 match.
    - `grep -F "Visit pbs.org/american-portrait" src/routes/pbs-american-portrait/+page.svelte` returns at least 1 match.
    - `grep -F '<h2 class="mt-12 text-lg font-medium">Stories</h2>' src/routes/pbs-american-portrait/+page.svelte` returns at least 1 match (verbatim h2).
    - `grep -F "pbsCollectionUrl(video.description)" src/routes/pbs-american-portrait/+page.svelte` returns at least 2 matches (one in {#if}, one in the href).
    - `grep -F "See on PBS" src/routes/pbs-american-portrait/+page.svelte` returns at least 1 match.
    - `grep -F "18 stories produced by Michelle Ngo" src/routes/pbs-american-portrait/+page.svelte` returns at least 1 match.
    - The verbatim approved paragraph from 05-01-PLAN.md `<approved>...</approved>` appears in `src/routes/pbs-american-portrait/+page.svelte` (verify with `grep -F` on the first 40 chars of the approved text).
    - The literal `{APPROVED_PBS_PARAGRAPH_VERBATIM_FROM_05-01-PLAN.md}` placeholder is GONE from the file.
    - `pnpm vitest run src/routes/pbs-american-portrait/` exits 0 (all helper tests + all page tests pass).
    - `pnpm check` exits 0.
    - `pnpm build` exits 0 — produces `build/pbs-american-portrait/index.html`.
    - `pnpm test:prerender` exits 0 — PBS landing file detected by the threshold added in Plan 05-01.
  </acceptance_criteria>
  <done>Real PBS landing renders at /pbs-american-portrait/ with verbatim copy. 15/18 cards show "See on PBS →" badges. All helper + page tests pass. Build artifact exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire cross-links — D-04 on /work/[category] (PBS only) + D-05 on /watch/[id] (PBS only)</name>
  <files>
    src/routes/work/[category]/+page.svelte,
    src/routes/work/[category]/page.test.ts,
    src/routes/watch/[id]/+page.svelte,
    src/routes/watch/[id]/page.test.ts
  </files>
  <read_first>
    - src/routes/work/[category]/+page.svelte (current shape — line 17-30 has the h1 + grid; D-04 insert goes between h1 and grid)
    - src/routes/watch/[id]/+page.svelte (current shape — D-35 metadata block is lines 59-68; D-05 insert goes immediately after the description block)
    - .planning/phases/05-pbs-american-portrait/05-CONTEXT.md (D-04, D-05 — exact text + placement + styling)
    - .planning/phases/05-pbs-american-portrait/05-RESEARCH.md §Code Examples 4 (D-04 insert shape)
  </read_first>
  <behavior>
    /work/[category]/+page.svelte:
    - When `data.category === 'PBS American Portrait'`, render an inline `<p><a href={`${base}/pbs-american-portrait/`}>→ About the PBS American Portrait project</a></p>` immediately AFTER the h1 and BEFORE the grid.
    - For all other 7 categories: render unchanged (no cross-link).

    /watch/[id]/+page.svelte:
    - When `video.category === 'PBS American Portrait'`, render an inline `<p><a href={`${base}/pbs-american-portrait/`}>→ About the PBS American Portrait project</a></p>` immediately AFTER the description block (D-35 item 5) and BEFORE the rail (D-36).
    - For all other categories: render unchanged.

    Plan 05-01 Task 3 added describe.skip blocks for both — those flip to describe (one-rule rename).

    Important: `/work/[category]/+page.svelte` does NOT currently import `base` from `$app/paths`. You ADD that import. `/watch/[id]/+page.svelte` already imports `base`.
  </behavior>
  <action>
    --- EDIT 1: src/routes/work/[category]/+page.svelte ---

    A) In the `<script lang="ts">` block (lines 5-11), ADD the `base` import + ESLint disable. The current script:
    ```svelte
    <script lang="ts">
      import type { PageData } from './$types';
      import VideoCard from '$lib/components/VideoCard.svelte';
      import { categoryAccent } from '$lib/components/categoryAccent';

      let { data }: { data: PageData } = $props();
    </script>
    ```

    REPLACE with:
    ```svelte
    <script lang="ts">
      /* eslint-disable svelte/no-navigation-without-resolve */
      import type { PageData } from './$types';
      import { base } from '$app/paths';
      import VideoCard from '$lib/components/VideoCard.svelte';
      import { categoryAccent } from '$lib/components/categoryAccent';

      let { data }: { data: PageData } = $props();
    </script>
    ```

    B) Between the existing `<h1>...</h1>` block (lines 18-24) and the `<ul ...>` grid (line 25), INSERT the D-04 conditional cross-link. The current section:
    ```svelte
    <section class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-8">
      <h1
        class="text-2xl md:text-3xl font-bold uppercase tracking-wider mb-6 {categoryAccent(
          data.category,
        )}"
      >
        {data.category} ({data.videos.length})
      </h1>
      <ul class="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-2 sm:gap-3">
    ```

    REPLACE with:
    ```svelte
    <section class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-8">
      <h1
        class="text-2xl md:text-3xl font-bold uppercase tracking-wider mb-6 {categoryAccent(
          data.category,
        )}"
      >
        {data.category} ({data.videos.length})
      </h1>
      {#if data.category === 'PBS American Portrait'}
        <!-- Phase 5 D-04: cross-link to the dedicated PBS landing page. -->
        <p class="-mt-4 mb-6 text-sm text-neutral-400">
          <a href={`${base}/pbs-american-portrait/`} class="hover:underline">
            → About the PBS American Portrait project
          </a>
        </p>
      {/if}
      <ul class="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-2 sm:gap-3">
    ```

    --- EDIT 2: src/routes/watch/[id]/+page.svelte ---

    The existing D-35 metadata block (lines 59-68):
    ```svelte
    <div class="mx-auto max-w-7xl mt-6 space-y-2">
      <h1 class="text-2xl md:text-3xl font-medium">{video.title}</h1>
      <CategoryTag category={video.category} href={`${base}/work/${categorySlug}`} />
      <p class="text-sm text-neutral-400">{video.uploader} · {year}</p>
      {#if video.description}
        <p class="text-sm text-neutral-300 whitespace-pre-line max-w-3xl pt-2">
          {video.description}
        </p>
      {/if}
    </div>
    ```

    INSERT the D-05 cross-link block immediately AFTER the closing `</div>` of the metadata block, before the `{#if rail.length > 0}` section that opens the rail. Result:
    ```svelte
    <div class="mx-auto max-w-7xl mt-6 space-y-2">
      <h1 class="text-2xl md:text-3xl font-medium">{video.title}</h1>
      <CategoryTag category={video.category} href={`${base}/work/${categorySlug}`} />
      <p class="text-sm text-neutral-400">{video.uploader} · {year}</p>
      {#if video.description}
        <p class="text-sm text-neutral-300 whitespace-pre-line max-w-3xl pt-2">
          {video.description}
        </p>
      {/if}
      {#if video.category === 'PBS American Portrait'}
        <!-- Phase 5 D-05: cross-link to the dedicated PBS landing (PBS videos only). -->
        <p class="text-sm text-neutral-400 pt-2">
          <a href={`${base}/pbs-american-portrait/`} class="hover:underline">
            → About the PBS American Portrait project
          </a>
        </p>
      {/if}
    </div>
    ```

    No new imports needed — `base` is already imported (line 23 of the current file).

    --- EDIT 3: src/routes/work/[category]/page.test.ts — flip .skip ---

    `describe.skip('/work/[category] — Phase 5 D-04 PBS cross-link'` → `describe('/work/[category] — Phase 5 D-04 PBS cross-link'`

    --- EDIT 4: src/routes/watch/[id]/page.test.ts — flip .skip ---

    `describe.skip('/watch/[id] — Phase 5 D-05 PBS cross-link'` → `describe('/watch/[id] — Phase 5 D-05 PBS cross-link'`

    --- VALIDATION ---
    ```
    pnpm vitest run src/routes/work/[category]/page.test.ts src/routes/watch/[id]/page.test.ts --reporter=basic
    pnpm check
    pnpm lint src/routes/work/[category]/+page.svelte src/routes/watch/[id]/+page.svelte
    pnpm build
    pnpm test:prerender
    ```

    All MUST exit 0.
  </action>
  <verify>
    <automated>pnpm test --reporter=basic 2>&1 | tail -30 &amp;&amp; pnpm check 2>&amp;1 | tail -10 &amp;&amp; pnpm lint 2>&amp;1 | tail -10 &amp;&amp; pnpm build 2>&amp;1 | tail -10 &amp;&amp; pnpm test:prerender</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "import { base } from '\$app/paths';" src/routes/work/[category]/+page.svelte` returns at least 1 match (new import added).
    - `grep -F "eslint-disable svelte/no-navigation-without-resolve" src/routes/work/[category]/+page.svelte` returns at least 1 match (new disable added).
    - `grep -F "About the PBS American Portrait project" src/routes/work/[category]/+page.svelte` returns at least 1 match (cross-link text).
    - `grep -F "Phase 5 D-04" src/routes/work/[category]/+page.svelte` returns at least 1 match (comment).
    - `grep -F "About the PBS American Portrait project" src/routes/watch/[id]/+page.svelte` returns at least 1 match (cross-link text).
    - `grep -F "Phase 5 D-05" src/routes/watch/[id]/+page.svelte` returns at least 1 match (comment).
    - `grep -F "video.category === 'PBS American Portrait'" src/routes/watch/[id]/+page.svelte` returns at least 1 match (conditional gate).
    - `grep -F "data.category === 'PBS American Portrait'" src/routes/work/[category]/+page.svelte` returns at least 1 match (conditional gate).
    - `grep -F "describe.skip" src/routes/work/[category]/page.test.ts` returns 0 matches.
    - `grep -F "describe.skip" src/routes/watch/[id]/page.test.ts` returns 0 matches.
    - `pnpm test` exits 0 (entire test suite — every previously-skipped Phase 5 assertion is now unskipped AND passing).
    - `pnpm check` exits 0.
    - `pnpm lint` exits 0.
    - `pnpm build` exits 0.
    - `pnpm test:prerender` exits 0.
    - The build output contains BOTH `build/pbs-american-portrait/index.html` (new landing) AND `build/work/pbs-american-portrait/index.html` (preserved filter route — PBS-03 parity).
  </acceptance_criteria>
  <done>Both cross-links wired with PBS-only gating. All Phase 5 tests green. Full validation suite (test + check + lint + build + prerender) exits 0. PBS-01, PBS-02, PBS-03 all observable: navigate to /pbs-american-portrait/ via TopNav (PBS-01), see 18 cards + context block (PBS-02), follow `/work/pbs-american-portrait/` from URL or cross-link to confirm filter parity (PBS-03).</done>
</task>

</tasks>

<verification>
After all 3 tasks complete:

1. **Full test suite green:**
   ```
   pnpm test --reporter=basic   # exit 0; zero .skip in Phase 5 files
   pnpm check                    # exit 0
   pnpm lint                     # exit 0
   ```

2. **Production build clean:**
   ```
   pnpm build                    # exit 0
   pnpm test:prerender           # exit 0
   ls build/pbs-american-portrait/index.html       # exists
   ls build/work/pbs-american-portrait/index.html  # exists (PBS-03 parity preserved)
   ```

3. **Manual smoke (operator):**
   - Run `pnpm preview` and open the staging URL
   - From home `/`, click the PBS American Portrait link in TopNav → URL becomes `/pbs-american-portrait/`
   - Confirm: h1 in cat-pbs accent color; subtitle; blockquote; outbound link opens pbs.org in new tab; h2 "Stories"; 18-card grid; ≥15 "See on PBS →" badges visible
   - TopNav PBS link should be highlighted (cat-pbs color) on /pbs-american-portrait/
   - Navigate to `/work/pbs-american-portrait/` (paste URL or use the TopNav PBS link's URL bar history) → confirm: 18 cards (same set); TopNav PBS link STILL highlighted; small inline cross-link "→ About the PBS American Portrait project" above the grid
   - Click any PBS card → `/watch/[id]/` → confirm cross-link "→ About the PBS American Portrait project" below the description; rail still renders
   - Click cross-link → returns to `/pbs-american-portrait/`
   - Visit `/watch/[id]` for a NON-PBS video (e.g., the producer reel) → confirm NO cross-link below description (negative)

4. **Coverage check:** All 3 phase requirements have GREEN automated tests:
   - PBS-01 → src/routes/pbs-american-portrait/page.test.ts (load + render) + src/lib/components/TopNav.test.ts (link target + active-state) + scripts/test-prerender-coverage.mjs (build artifact)
   - PBS-02 → src/routes/pbs-american-portrait/page.test.ts (18 cards, context block elements) + src/routes/pbs-american-portrait/_pbsCollectionUrl.test.ts (helper)
   - PBS-03 → src/routes/work/[category]/page.test.ts (existing 18-card load assertion + new cross-link assertion) + scripts/test-prerender-coverage.mjs (build/work/pbs-american-portrait/index.html still emitted)
</verification>

<success_criteria>
- [ ] TopNav PBS link href === `/pbs-american-portrait/` (D-02).
- [ ] TopNav PBS link highlighted on BOTH `/pbs-american-portrait/` AND `/work/pbs-american-portrait/` (D-03).
- [ ] /pbs-american-portrait/ renders h1 with text-cat-pbs, subtitle, verbatim PBS blockquote, attribution, outbound link, h2 "Stories", 18-card grid in D-18 order (D-08..D-20, PBS-01, PBS-02).
- [ ] 15/18 PBS cards render a "See on PBS →" badge with the correct collection URL (D-21); 3 PBS cards omit the badge (audit-verified ids).
- [ ] /work/pbs-american-portrait/ renders the D-04 cross-link after the h1 (PBS-03 parity preserved + new cross-link).
- [ ] /watch/[id] for PBS videos renders the D-05 cross-link below the description; non-PBS videos do NOT render it.
- [ ] All Phase 5 test stubs unskipped and passing (`grep -c "describe.skip" src/routes/pbs-american-portrait/ src/lib/components/TopNav.test.ts src/routes/watch/[id]/page.test.ts src/routes/work/[category]/page.test.ts` returns 0).
- [ ] `pnpm test`, `pnpm check`, `pnpm lint`, `pnpm build`, `pnpm test:prerender` all exit 0.
- [ ] `build/pbs-american-portrait/index.html` AND `build/work/pbs-american-portrait/index.html` both present.
</success_criteria>

<output>
After completion, create `.planning/phases/05-pbs-american-portrait/05-02-SUMMARY.md` capturing:
  - Files modified with line-count deltas
  - The exact PBS verbatim copy paragraph that landed in +page.svelte (verbatim text + which candidate A/B/C)
  - Confirmation that all 5 validation commands exit 0
  - Any deviations from this PLAN (e.g., if `pnpm test:prerender` revealed a missing artifact, the diagnostic + fix)
  - Manual smoke checklist results
</output>
