---
phase: 05-pbs-american-portrait
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/pbs-american-portrait/+page.svelte
  - src/routes/pbs-american-portrait/+page.ts
  - src/routes/pbs-american-portrait/_pbsCollectionUrl.ts
  - src/routes/pbs-american-portrait/_pbsCollectionUrl.test.ts
  - src/routes/pbs-american-portrait/page.test.ts
  - src/lib/components/TopNav.test.ts
  - src/routes/watch/[id]/page.test.ts
  - src/routes/work/[category]/page.test.ts
  - scripts/test-prerender-coverage.mjs
autonomous: false
requirements: [PBS-02]
nyquist_compliant: true

must_haves:
  truths:
    - "User reviewing the plan can see all 3 verbatim PBS copy candidates and lock one before any code is written"
    - "Every Phase 5 test assertion (route render, helper extraction, TopNav extension, cross-links, prerender coverage) has a RED-by-skip stub committed before any implementation runs"
    - "Approved PBS verbatim paragraph is captured in this PLAN.md inline so 05-02 implementer reads it directly (no re-fetching, no re-deciding)"
  artifacts:
    - path: ".planning/phases/05-pbs-american-portrait/05-01-PLAN.md"
      provides: "Approved verbatim PBS copy (single paragraph) locked under <approved_pbs_copy>"
      contains: "<approved_pbs_copy>"
    - path: "src/routes/pbs-american-portrait/+page.ts"
      provides: "Stub load() returning { videos: [], pbsBlurb: '' } so import { load } in stubbed test resolves"
      exports: ["load"]
    - path: "src/routes/pbs-american-portrait/+page.svelte"
      provides: "Stub component that satisfies vitest mount() — renders nothing meaningful; tests are describe.skip"
    - path: "src/routes/pbs-american-portrait/_pbsCollectionUrl.ts"
      provides: "Stub exporting pbsCollectionUrl(): null so helper-test import resolves"
      exports: ["pbsCollectionUrl"]
    - path: "src/routes/pbs-american-portrait/_pbsCollectionUrl.test.ts"
      provides: "RED-by-skip stubs for 15 positive extractions + 3 null + 4 edge cases"
      contains: "describe.skip"
    - path: "src/routes/pbs-american-portrait/page.test.ts"
      provides: "RED-by-skip stubs across 6 describe blocks (render / blockquote / outbound / subtitle / badges / sort)"
      contains: "describe.skip"
    - path: "src/lib/components/TopNav.test.ts"
      provides: "Existing tests unchanged; THREE new describe.skip assertions added (D-02 href, D-03 active-state on /pbs-american-portrait/, D-03 regression on /work/pbs-american-portrait/)"
      contains: "describe.skip"
    - path: "src/routes/watch/[id]/page.test.ts"
      provides: "Existing tests unchanged; TWO new describe.skip assertions added (PBS cross-link present, non-PBS cross-link absent)"
      contains: "describe.skip"
    - path: "src/routes/work/[category]/page.test.ts"
      provides: "Existing tests unchanged; TWO new describe.skip assertions added (PBS cross-link present, non-PBS cross-link absent)"
      contains: "describe.skip"
    - path: "scripts/test-prerender-coverage.mjs"
      provides: "Threshold check extended to assert build/pbs-american-portrait/index.html exists; remains exit 0 today because route doesn't exist yet (will only fire after 05-02 build)"
      contains: "pbs-american-portrait"
  key_links:
    - from: ".planning/phases/05-pbs-american-portrait/05-01-PLAN.md"
      to: "05-02-PLAN.md"
      via: "<approved_pbs_copy> block referenced verbatim in 05-02 Task 2 action"
      pattern: "approved_pbs_copy"
    - from: "src/routes/pbs-american-portrait/page.test.ts"
      to: "src/routes/pbs-american-portrait/+page.svelte"
      via: "static import + mount()"
      pattern: "import Page from './\\+page.svelte'"
    - from: "src/routes/pbs-american-portrait/_pbsCollectionUrl.test.ts"
      to: "src/routes/pbs-american-portrait/_pbsCollectionUrl.ts"
      via: "static import"
      pattern: "import \\{ pbsCollectionUrl \\} from './_pbsCollectionUrl'"
---

<objective>
Plan 05-01 lays the foundation for Phase 5 with two outputs:

  1. **User-approved PBS verbatim copy** — surfaced as 3 candidate paragraphs (verbatim-fetched from pbs.org/american-portrait at plan time per CONTEXT D-11). User picks ONE at the Task 1 checkpoint; the approved paragraph is committed back into this PLAN.md under <approved_pbs_copy> so Plan 05-02 can read it without re-fetching.

  2. **Complete Wave 0 test scaffolding** — all RED-by-skip stubs that Plan 05-02 will flip to GREEN. Covers the new PBS route (load + render + helper + 15 badges + sort), the TopNav extension (D-02 retarget + D-03 active-state), the two cross-links (D-04 + D-05), and the prerender coverage script threshold update.

Purpose: Phase 5 has ONE user-gated decision (the verbatim PBS copy) and a fully-locked test contract. Resolving both before any production code is written removes both ambiguity and back-and-forth from the build wave (05-02).

Output:
  - 1 user-approved PBS paragraph (captured inline under <approved_pbs_copy>)
  - 4 new files (+page.ts stub, +page.svelte stub, _pbsCollectionUrl.ts stub, _pbsCollectionUrl.test.ts stub)
  - 1 new test file (page.test.ts with describe.skip stubs across 6 suites)
  - 3 modified test files (TopNav.test.ts, watch/[id]/page.test.ts, work/[category]/page.test.ts — additive describe.skip blocks only)
  - 1 modified script (scripts/test-prerender-coverage.mjs — threshold extension)
  - Zero behavior change: `pnpm test` exit 0 (all new tests are .skip); `pnpm build` exit 0 (no route added yet — _-prefix excludes helper from routing); `pnpm test:prerender` exit 0 (threshold check skips when route absent)
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-pbs-american-portrait/05-CONTEXT.md
@.planning/phases/05-pbs-american-portrait/05-RESEARCH.md
@.planning/phases/05-pbs-american-portrait/05-VALIDATION.md

# Phase 3 + 4 contracts (carry-forward — read for prior-art test patterns)
@src/lib/components/TopNav.svelte
@src/lib/components/TopNav.test.ts
@src/routes/watch/[id]/+page.svelte
@src/routes/watch/[id]/page.test.ts
@src/routes/work/[category]/+page.svelte
@src/routes/work/[category]/page.test.ts
@scripts/test-prerender-coverage.mjs

<interfaces>
<!-- Locked APIs Plan 05-01 stubs MUST honor (executor uses these directly — no codebase exploration needed). -->

From src/lib/data/index.ts:
```typescript
export type { Video } from './schema';
export type { Category } from './categories';
export { CATEGORIES, categoryToSlug, slugToCategory } from './categories';
export {
  videos,             // readonly Video[] (56 records, hidden-filtered per Phase 2 D-14)
  producerReelId,
  getById,            // (id: string) => Video | undefined
  getByCategory,      // (category: Category) => readonly Video[]
  getCategoriesInDisplayOrder,
  getCategoriesWithCounts,
} from './videos';
```

From src/lib/components/categoryAccent.ts:
```typescript
export function categoryAccent(category: Category): string;
// categoryAccent('PBS American Portrait') === 'text-cat-pbs'   (NOT 'text-cat-pbs-american-portrait' — see RESEARCH Pitfall 7)
```

From src/lib/data/categories.ts:
```typescript
// categoryToSlug('PBS American Portrait') === 'pbs-american-portrait'
```

Vitest mount() pattern in use across project (TopNav.test.ts):
```typescript
import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest';
const { mockPage } = vi.hoisted(() => ({ mockPage: { url: new URL('http://localhost/'), route: { id: '/' as string | null } } }));
vi.mock('$app/state', () => ({ page: mockPage }));
vi.mock('$app/paths', () => ({ base: '' }));
import { mount, unmount } from 'svelte';
// ... later: component = mount(Page, { target: makeHost(), props: { data } });
```

callLoad narrow helper (from src/routes/watch/[id]/page.test.ts lines 11-18 — REUSE this shape):
```typescript
async function callLoad(
  event: Parameters<typeof load>[0]
): Promise<{ videos: Video[] }> {
  const result = await load(event);
  if (!result) throw new Error('load() returned void');
  return result as { videos: Video[] };
}
```

Vitest project routing (vite.config.ts test.projects):
  - `data` (node env): runs `src/lib/data/**/*.{test,spec}.{js,ts}`
  - `ui` (jsdom env, browser conditions): runs `src/lib/components/**/*.{test,spec}.{js,ts}` AND `src/routes/**/*.{test,spec}.{js,ts}`
  - All NEW Phase 5 test files (page.test.ts, _pbsCollectionUrl.test.ts) live under `src/routes/pbs-american-portrait/` → picked up by the `ui` project.
</interfaces>
</context>

<pbs_copy_candidates>
<!--
  CONTEXT.md D-11 GATE: the planner fetched pbs.org/american-portrait/ at plan time
  and surfaced the 3 verbatim paragraphs below. Task 1 below is a user-gated
  checkpoint that locks ONE choice before any test stub gets the literal text
  baked into it.

  Source: https://www.pbs.org/american-portrait/ — fetched 2026-05-11 by planner.
  These paragraphs are verbatim, no synthesis, no edits.
-->

**Candidate A — Mission (1 sentence, 20 words) — punchy, tagline tone**

> PBS American Portrait invites America to participate in a national conversation about what it really means to be an American today.

**Candidate B — Mission + Mechanism (2 sentences, ~47 words) — fuller context**

> PBS American Portrait invites America to participate in a national conversation about what it really means to be an American today. To answer this question, PBS and its partners collected thousands of photos, videos and text submissions from across America to capture the state and spirit of our nation.

**Candidate C — Editorial (2 sentences, ~50 words) — most blockquote-worthy, complements "Stories" h2**

> Whether it's joy or sorrow, triumph or hardship, family traditions followed for decades or just the chaos of the morning school run, PBS American Portrait put together a picture of life as it's really lived. The show gives a glimpse into American life, and a chance for everyday Americans to be heard.

**Planner recommendation:** **Candidate C** (default). Reads as a blockquote, complements the `<h2>Stories</h2>` heading below it semantically, and lands the editorial-minimal aesthetic the page is going for. **Candidate A** as the brevity fallback.

</pbs_copy_candidates>

<approved_pbs_copy>
<!--
  After Task 1 user pick, the executor REPLACES this entire comment block with the
  approved paragraph verbatim, wrapped in <approved>...</approved> tags. Plan 05-02
  Task 2 reads `<approved>...</approved>` text directly and embeds it inline in
  +page.svelte. Format MUST be:

      <approved>
      {verbatim PBS paragraph, no quotation marks, no leading/trailing whitespace}
      </approved>

  Do NOT delete this comment; only add the <approved>...</approved> sibling below it.
-->
</approved_pbs_copy>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: User locks verbatim PBS copy</name>
  <read_first>
    - .planning/phases/05-pbs-american-portrait/05-01-PLAN.md (this file, the <pbs_copy_candidates> block above)
    - .planning/phases/05-pbs-american-portrait/05-CONTEXT.md (D-10, D-11 — verbatim requirement; attribution shape)
    - .planning/phases/05-pbs-american-portrait/05-RESEARCH.md (§State of the Art — full candidate context + planner recommendation)
  </read_first>
  <decision>
    Which verbatim PBS American Portrait paragraph goes inside the `<blockquote>` on /pbs-american-portrait/?
  </decision>
  <context>
    The CONTEXT D-11 gate is explicit: "Planner fetches pbs.org/american-portrait/ at plan time, normalizes the project description (single paragraph), and surfaces the exact paragraph verbatim in PLAN.md for user approval BEFORE execution." This task is that user-approval gate.

    The 3 candidates above are verbatim from https://www.pbs.org/american-portrait/. No synthesis, no edits. After approval, the executor REPLACES the empty <approved_pbs_copy> block with `<approved>{paragraph}</approved>` containing the chosen paragraph verbatim (no quotation marks, no leading/trailing whitespace). Plan 05-02 Task 2 reads that approved text and embeds it inline in +page.svelte (D-11 says inline literal — no separate strings file because i18n is OOS).
  </context>
  <options>
    <option id="option-a">
      <name>Candidate A — Mission (20 words, 1 sentence)</name>
      <pros>Shortest. Punchy. Reads as a clear project mission statement. Highest signal-to-noise.</pros>
      <cons>Doesn't describe what the project actually did (no mechanism); doesn't paint a picture of the content.</cons>
    </option>
    <option id="option-b">
      <name>Candidate B — Mission + Mechanism (~47 words, 2 sentences)</name>
      <pros>Combines tagline + the "what they did" sentence. Maximal context in a still-short blockquote.</pros>
      <cons>Reads more like a press paragraph than a blockquote-worthy moment. Less editorial.</cons>
    </option>
    <option id="option-c">
      <name>Candidate C — Editorial (~50 words, 2 sentences) [PLANNER DEFAULT]</name>
      <pros>Most blockquote-worthy ("joy or sorrow, triumph or hardship, family traditions..." paints a picture). Complements the &lt;h2&gt;Stories&lt;/h2&gt; below it semantically. Editorial-minimal aesthetic.</pros>
      <cons>Doesn't open with the project name explicitly (the &lt;h1&gt; carries that — fine).</cons>
    </option>
  </options>
  <action>
    1. Print the <pbs_copy_candidates> block to the user (already verbatim above in this PLAN.md).
    2. Print the planner recommendation: "Candidate C (editorial) — default; Candidate A (mission) as brevity fallback."
    3. Wait for user pick. Accepted responses:
       - "option-a" / "A" / "candidate a" / "1" → Candidate A
       - "option-b" / "B" / "candidate b" / "2" → Candidate B
       - "option-c" / "C" / "candidate c" / "3" → Candidate C
    4. After user picks, edit `.planning/phases/05-pbs-american-portrait/05-01-PLAN.md`:
       - Find the empty `<approved_pbs_copy>` block (currently contains ONLY the planner comment).
       - Insert an `<approved>...</approved>` element directly after the closing `-->` of the planner comment, containing the chosen paragraph verbatim (the exact text from the chosen candidate block above, minus the `> ` blockquote markdown prefix). NO quotation marks added. NO whitespace before/after the text. Single paragraph (Candidate B and C carry one space between their two sentences — that's part of the verbatim text).
       - Save.
    5. Confirm to the user: "Locked: Candidate {X}. Approved paragraph captured in 05-01-PLAN.md under <approved_pbs_copy>. Plan 05-02 Task 2 will embed it verbatim in +page.svelte."
  </action>
  <acceptance_criteria>
    - `.planning/phases/05-pbs-american-portrait/05-01-PLAN.md` contains an `<approved>` element inside the `<approved_pbs_copy>` block (grep: `<approved>` returns at least 1 match in this file).
    - The text inside `<approved>...</approved>` matches one of the 3 candidates BYTE-FOR-BYTE except for the leading `> ` markdown blockquote prefix (which is stripped). Verify via `grep -F` for the first 30 characters of the chosen paragraph.
    - No additional candidates remain pasted under `<approved_pbs_copy>` — exactly ONE `<approved>` element.
    - No backslashes, no escaped characters, no added quotation marks.
  </acceptance_criteria>
  <resume-signal>Reply with: option-a, option-b, or option-c (or letter/name equivalent).</resume-signal>
  <verify>
    <automated>node -e "const f=require('fs').readFileSync('.planning/phases/05-pbs-american-portrait/05-01-PLAN.md','utf-8'); const m=f.match(/&lt;approved&gt;([\\s\\S]*?)&lt;\\/approved&gt;/); if(!m){console.error('FAIL: no &lt;approved&gt; block');process.exit(1)}; const t=m[1].trim(); if(t.length&lt;20){console.error('FAIL: approved text too short');process.exit(1)}; console.log('PASS: approved length',t.length)"</automated>
  </verify>
  <done>User has selected one of A/B/C and the verbatim paragraph is captured inside an `<approved>...</approved>` element under `<approved_pbs_copy>` in this PLAN.md file. The automated check above passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scaffold PBS route stubs + Wave 0 test scaffolding</name>
  <files>
    src/routes/pbs-american-portrait/+page.ts,
    src/routes/pbs-american-portrait/+page.svelte,
    src/routes/pbs-american-portrait/_pbsCollectionUrl.ts,
    src/routes/pbs-american-portrait/_pbsCollectionUrl.test.ts,
    src/routes/pbs-american-portrait/page.test.ts
  </files>
  <read_first>
    - .planning/phases/05-pbs-american-portrait/05-RESEARCH.md §Validation Architecture §Wave 0 Gaps (test inventory) + §Code Examples 1-2 (stub shapes) + §Code Examples 5 (18-record audit — three null-record ids)
    - .planning/phases/05-pbs-american-portrait/05-CONTEXT.md §Page Composition (D-14..D-17) §Grid & Per-Card Extras (D-18..D-22)
    - src/routes/watch/[id]/page.test.ts (REUSE the `callLoad` narrow-helper pattern verbatim — see <interfaces> block above)
    - src/routes/work/[category]/page.test.ts (REUSE the same helper shape)
    - src/lib/components/TopNav.test.ts (REUSE the `vi.hoisted` + `vi.mock('$app/state')` + `mount()` + `flushSync` pattern for jsdom route tests)
    - src/routes/watch/[id]/+page.ts (REUSE the `async PageLoad` + return shape; no params, no error() for the PBS landing)
    - src/routes/+page.ts (Phase 4 home — REUSE the sync `load: PageLoad = () => ({ ... })` shape — PBS landing has no params + no rejection contract, sync is fine)
    - src/lib/data/videos.json (audit: 18 PBS records; 3 have NO collection URL — ids 620232398, 1007061884, 1007027015)
  </read_first>
  <behavior>
    All test stubs are RED-by-skip (describe.skip) so they import cleanly but contribute zero pass/fail signal. Stub PRODUCTION files (+page.ts, +page.svelte, _pbsCollectionUrl.ts) export the symbols the tests import, but with placeholder return values so vitest doesn't crash.

    - Test 1 (helper, 15 positive): `pbsCollectionUrl(description)` returns the collection URL for each of 15 PBS records that have one — list locked below.
    - Test 2 (helper, 3 null): `pbsCollectionUrl(description)` returns `null` for the 3 records WITHOUT a URL: `American Portrait Year in Review 2020` (id 620232398), `PBS American Portrait - Celebrity Message` (id 1007061884), `Introducing PBS American Portrait` (id 1007027015).
    - Test 3 (helper, edge): Trailing punctuation `.`, `,`, `!`, `?`, `)` stripped from URL match.
    - Test 4 (helper, edge): Multiple URLs in description → first match returned.
    - Test 5 (helper, edge): Non-pbs.org URL ignored (returns null when only off-domain URLs present).
    - Test 6 (helper, edge): Malformed protocol (e.g. `httpx://`) returns null.
    - Test 7 (route load): `load()` returns exactly 18 PBS videos (PBS-02).
    - Test 8 (route load): D-18 sort — first card is featured-first then date-desc (RESEARCH §Code Examples 6 verified order: featured PBS rows bubble to top; among non-featured, 2024-10-24 dated rows precede 2024-09-06 dated rows).
    - Test 9 (route render): Page renders exactly 18 `<a>` cards (one per VideoCard's anchor) — PBS-02.
    - Test 10 (route render): `<h1>` text === `PBS American Portrait`; class contains `text-cat-pbs` — D-08.
    - Test 11 (route render): Subtitle `<p>` text === `18 stories produced by Michelle Ngo` — D-09.
    - Test 12 (route render): `<blockquote>` exists; text content has length ≥ 20 chars (locks D-10).
    - Test 13 (route render): Outbound `<a href="https://www.pbs.org/american-portrait/">` has `target="_blank"` AND `rel="noopener"` — D-12.
    - Test 14 (route render): `<h2>` with text `Stories` exists — D-16.
    - Test 15 (route render): Exactly 15 "See on PBS →" badge `<a>` elements rendered (D-21 conditional on extractable URL) — RESEARCH §Pitfall 4.
  </behavior>
  <action>
    Create 5 files under `src/routes/pbs-american-portrait/` (NEW directory).

    --- FILE 1: src/routes/pbs-american-portrait/+page.ts (PRODUCTION STUB) ---
    ```ts
    /**
     * Phase 5 Plan 05-01 Wave 0 stub for /pbs-american-portrait/.
     *
     * Returns an empty videos array + empty pbsBlurb. Plan 05-02 Task 2 replaces
     * this with the real getByCategory('PBS American Portrait') + D-18 sort.
     *
     * Sync (no async): no error() throw, no rejection contract — mirrors /+page.ts (Phase 4).
     */
    import type { PageLoad } from './$types';

    export const load: PageLoad = () => ({
      videos: [] as never[],
      pbsBlurb: '',
    });
    ```

    --- FILE 2: src/routes/pbs-american-portrait/+page.svelte (PRODUCTION STUB) ---
    ```svelte
    <!--
      Phase 5 Plan 05-01 Wave 0 stub for /pbs-american-portrait/.
      Plan 05-02 Task 2 replaces this entire file with the real layout.
      For now: minimal markup so `mount(Page, { props: { data } })` doesn't crash
      and ALL Wave 0 page.test.ts assertions can be RED-by-skip.
    -->
    <script lang="ts">
      import type { PageData } from './$types';
      let { data }: { data: PageData } = $props();
    </script>

    <section>
      <!-- intentionally minimal — flipped to full layout in 05-02 Task 2 -->
      <p>PBS American Portrait — stub ({data.videos.length} videos)</p>
    </section>
    ```

    --- FILE 3: src/routes/pbs-american-portrait/_pbsCollectionUrl.ts (PRODUCTION STUB) ---
    ```ts
    /**
     * Phase 5 Plan 05-01 Wave 0 stub. Real regex extraction lands in 05-02 Task 3.
     *
     * Underscore prefix excludes this file from SvelteKit route detection.
     * Plan 05-02 replaces the body with:
     *   const COLLECTION_URL = /https?:\/\/(?:www\.)?pbs\.org\/american-portrait\/collection\/[^\s)]+/;
     *   const TRAILING_PUNCT = /[).,!?]+$/;
     */
    // eslint-disable-next-line @typescript-eslint/no-unused-vars
    export function pbsCollectionUrl(description: string): string | null {
      return null;
    }
    ```

    --- FILE 4: src/routes/pbs-american-portrait/_pbsCollectionUrl.test.ts (TEST STUBS — RED-by-skip) ---
    ```ts
    import { describe, expect, it } from 'vitest';
    import { pbsCollectionUrl } from './_pbsCollectionUrl';
    import { videos } from '$lib/data';

    // The 18 PBS records: 15 have a pbs.org/american-portrait/collection/... URL
    // embedded in description; 3 do not. Audit-verified ids (RESEARCH §Code Examples 5):
    const PBS_NULL_IDS = ['620232398', '1007061884', '1007027015'] as const;

    describe.skip('pbsCollectionUrl — 15 positive extractions (D-21)', () => {
      it('extracts a https://www.pbs.org/american-portrait/collection/... URL from each of the 15 records that contain one', () => {
        const pbs = videos.filter((v) => v.category === 'PBS American Portrait');
        const withUrl = pbs.filter((v) => !PBS_NULL_IDS.includes(v.id as (typeof PBS_NULL_IDS)[number]));
        expect(withUrl.length).toBe(15);
        for (const v of withUrl) {
          const url = pbsCollectionUrl(v.description);
          expect(url, `expected URL for video id ${v.id}`).toMatch(
            /^https?:\/\/(?:www\.)?pbs\.org\/american-portrait\/collection\//
          );
        }
      });
    });

    describe.skip('pbsCollectionUrl — returns null for 3 records without URL (Pitfall 4)', () => {
      it('returns null for American Portrait Year in Review 2020 (id 620232398)', () => {
        const v = videos.find((x) => x.id === '620232398');
        expect(v, 'fixture missing').toBeDefined();
        expect(pbsCollectionUrl(v!.description)).toBeNull();
      });
      it('returns null for PBS American Portrait - Celebrity Message (id 1007061884)', () => {
        const v = videos.find((x) => x.id === '1007061884');
        expect(v, 'fixture missing').toBeDefined();
        expect(pbsCollectionUrl(v!.description)).toBeNull();
      });
      it('returns null for Introducing PBS American Portrait (id 1007027015)', () => {
        const v = videos.find((x) => x.id === '1007027015');
        expect(v, 'fixture missing').toBeDefined();
        expect(pbsCollectionUrl(v!.description)).toBeNull();
      });
    });

    describe.skip('pbsCollectionUrl — edge cases', () => {
      it('strips trailing period', () => {
        const url = pbsCollectionUrl(
          'See the collection at https://www.pbs.org/american-portrait/collection/57/i-began-to-live-my-truth/.'
        );
        expect(url).toBe('https://www.pbs.org/american-portrait/collection/57/i-began-to-live-my-truth/');
      });
      it('strips trailing comma', () => {
        const url = pbsCollectionUrl(
          'Visit https://www.pbs.org/american-portrait/collection/55/i-want-justice/, and watch more.'
        );
        expect(url).toBe('https://www.pbs.org/american-portrait/collection/55/i-want-justice/');
      });
      it('returns the first match when multiple PBS collection URLs present', () => {
        const url = pbsCollectionUrl(
          'Collection one: https://www.pbs.org/american-portrait/collection/1/a/ and two: https://www.pbs.org/american-portrait/collection/2/b/'
        );
        expect(url).toBe('https://www.pbs.org/american-portrait/collection/1/a/');
      });
      it('ignores non-pbs.org URLs', () => {
        expect(pbsCollectionUrl('Visit https://example.com/some/path/')).toBeNull();
      });
      it('returns null on malformed protocol', () => {
        expect(pbsCollectionUrl('See: httpx://www.pbs.org/american-portrait/collection/57/x/')).toBeNull();
      });
    });
    ```

    --- FILE 5: src/routes/pbs-american-portrait/page.test.ts (TEST STUBS — RED-by-skip) ---
    ```ts
    import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest';
    import { load } from './+page';
    import type { Video } from '$lib/data';

    // Mock $app/state + $app/paths BEFORE Page import (same pattern as TopNav.test.ts).
    const { mockPage } = vi.hoisted(() => ({
      mockPage: { url: new URL('http://localhost/pbs-american-portrait/'), route: { id: '/pbs-american-portrait' as string | null } },
    }));
    vi.mock('$app/state', () => ({ page: mockPage }));
    vi.mock('$app/paths', () => ({ base: '' }));

    import { mount, unmount } from 'svelte';
    import Page from './+page.svelte';

    // PageData narrower (mirrors src/routes/+page.ts test in Phase 4 Plan 04-05).
    async function callLoad(
      event: Parameters<typeof load>[0]
    ): Promise<{ videos: Video[] }> {
      const result = await load(event);
      if (!result) throw new Error('load() returned void');
      return result as { videos: Video[] };
    }

    let host: HTMLElement;
    let component: ReturnType<typeof mount> | undefined;
    beforeEach(() => {
      mockPage.url = new URL('http://localhost/pbs-american-portrait/');
      mockPage.route = { id: '/pbs-american-portrait' };
    });
    afterEach(() => {
      if (component) { unmount(component); component = undefined; }
      host?.remove();
    });
    function makeHost(): HTMLElement {
      host = document.createElement('div');
      document.body.appendChild(host);
      return host;
    }

    describe.skip('/pbs-american-portrait load — PBS-02 (D-18 sort, 18 cards)', () => {
      it('load returns 18 videos (all PBS)', async () => {
        const result = await callLoad({} as Parameters<typeof load>[0]);
        expect(result.videos.length).toBe(18);
        for (const v of result.videos) {
          expect((v as Video).category).toBe('PBS American Portrait');
        }
      });
      it('sort featured-first then published date desc (D-25)', async () => {
        const result = await callLoad({} as Parameters<typeof load>[0]);
        // Featured rows precede non-featured.
        let sawNonFeatured = false;
        for (const v of result.videos) {
          if (!(v as Video & { featured: boolean }).featured) sawNonFeatured = true;
          else if (sawNonFeatured) throw new Error('featured row after non-featured — sort violated');
        }
        // Within non-featured: dates monotonically non-increasing.
        const nonFeatured = result.videos.filter((v) => !(v as Video & { featured: boolean }).featured);
        for (let i = 1; i < nonFeatured.length; i++) {
          expect(nonFeatured[i - 1]!.published.localeCompare(nonFeatured[i]!.published)).toBeGreaterThanOrEqual(0);
        }
      });
    });

    describe.skip('/pbs-american-portrait render — PBS-02 (D-08, D-09, D-10, D-12, D-16, D-19)', () => {
      it('renders 18 cards', async () => {
        const data = await callLoad({} as Parameters<typeof load>[0]);
        component = mount(Page, { target: makeHost(), props: { data } });
        // VideoCard wraps each card in a single <a> with href /watch/[id] — count the anchors that match.
        const cardLinks = Array.from(host.querySelectorAll('a[href^="/watch/"]'));
        expect(cardLinks.length).toBe(18);
      });
      it('h1 has text-cat-pbs class and text "PBS American Portrait" (D-08)', async () => {
        const data = await callLoad({} as Parameters<typeof load>[0]);
        component = mount(Page, { target: makeHost(), props: { data } });
        const h1 = host.querySelector('h1');
        expect(h1?.textContent?.trim()).toBe('PBS American Portrait');
        expect(h1?.className).toMatch(/text-cat-pbs/);
      });
      it('subtitle text present "18 stories produced by Michelle Ngo" (D-09)', async () => {
        const data = await callLoad({} as Parameters<typeof load>[0]);
        component = mount(Page, { target: makeHost(), props: { data } });
        // Locate the paragraph whose textContent matches verbatim.
        const subtitleP = Array.from(host.querySelectorAll('p')).find(
          (p) => p.textContent?.trim() === '18 stories produced by Michelle Ngo'
        );
        expect(subtitleP).toBeDefined();
      });
      it('blockquote rendered with non-trivial body (D-10)', async () => {
        const data = await callLoad({} as Parameters<typeof load>[0]);
        component = mount(Page, { target: makeHost(), props: { data } });
        const bq = host.querySelector('blockquote');
        expect(bq).toBeDefined();
        expect((bq?.textContent?.trim().length ?? 0)).toBeGreaterThanOrEqual(20);
      });
      it('outbound link attrs: target=_blank rel=noopener (D-12)', async () => {
        const data = await callLoad({} as Parameters<typeof load>[0]);
        component = mount(Page, { target: makeHost(), props: { data } });
        const outbound = host.querySelector('a[href="https://www.pbs.org/american-portrait/"]');
        expect(outbound).toBeDefined();
        expect(outbound?.getAttribute('target')).toBe('_blank');
        expect(outbound?.getAttribute('rel')).toBe('noopener');
      });
      it('h2 "Stories" present (D-16)', async () => {
        const data = await callLoad({} as Parameters<typeof load>[0]);
        component = mount(Page, { target: makeHost(), props: { data } });
        const h2 = host.querySelector('h2');
        expect(h2?.textContent?.trim()).toBe('Stories');
      });
    });

    describe.skip('/pbs-american-portrait per-card PBS badges — PBS-02 (D-21)', () => {
      it('15 "See on PBS →" badges rendered (3 PBS videos have no collection URL)', async () => {
        const data = await callLoad({} as Parameters<typeof load>[0]);
        component = mount(Page, { target: makeHost(), props: { data } });
        // Per-card badge anchors point at pbs.org/american-portrait/collection/...
        const badges = Array.from(host.querySelectorAll('a[href*="pbs.org/american-portrait/collection/"]'));
        expect(badges.length).toBe(15);
        // All open in new tab with noopener.
        for (const b of badges) {
          expect(b.getAttribute('target')).toBe('_blank');
          expect(b.getAttribute('rel')).toBe('noopener');
          expect(b.textContent?.trim()).toContain('See on PBS');
        }
      });
    });
    ```

    Then run validation:
    ```
    pnpm test --reporter=basic
    pnpm check
    ```

    Both MUST exit 0. New tests are describe.skip so they contribute 0 fail signal.
  </action>
  <verify>
    <automated>pnpm test --reporter=basic 2>&1 | tail -20 &amp;&amp; pnpm check 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `src/routes/pbs-american-portrait/+page.ts` exists; `grep -F "export const load" src/routes/pbs-american-portrait/+page.ts` returns at least 1 match.
    - File `src/routes/pbs-american-portrait/+page.svelte` exists; `grep -F "let { data }" src/routes/pbs-american-portrait/+page.svelte` returns at least 1 match.
    - File `src/routes/pbs-american-portrait/_pbsCollectionUrl.ts` exists; `grep -F "export function pbsCollectionUrl" src/routes/pbs-american-portrait/_pbsCollectionUrl.ts` returns at least 1 match.
    - File `src/routes/pbs-american-portrait/_pbsCollectionUrl.test.ts` exists; `grep -c "describe.skip" src/routes/pbs-american-portrait/_pbsCollectionUrl.test.ts` returns 3.
    - File `src/routes/pbs-american-portrait/page.test.ts` exists; `grep -c "describe.skip" src/routes/pbs-american-portrait/page.test.ts` returns 3.
    - `pnpm test --reporter=basic` exits 0 (every existing test continues to pass; new tests are all .skip).
    - `pnpm check` exits 0 (no TypeScript errors).
    - No `@ts-expect-error` directives, no `// eslint-disable` lines beyond the one allowed in _pbsCollectionUrl.ts (the unused-vars stub).
  </acceptance_criteria>
  <done>5 files created. `pnpm test` and `pnpm check` both exit 0. Plan 05-02 can statically import `pbsCollectionUrl`, `load`, and `Page` against these stubs and run its own GREEN flips one-rule (`describe.skip` → `describe`).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend existing test files with Phase 5 RED-by-skip assertions + extend prerender script</name>
  <files>
    src/lib/components/TopNav.test.ts,
    src/routes/watch/[id]/page.test.ts,
    src/routes/work/[category]/page.test.ts,
    scripts/test-prerender-coverage.mjs
  </files>
  <read_first>
    - src/lib/components/TopNav.test.ts (full file — additive append only; reuse vi.hoisted + mockPage pattern)
    - src/routes/watch/[id]/page.test.ts (full file — additive append; existing callLoad already imports Video)
    - src/routes/work/[category]/page.test.ts (full file — additive append; existing callLoad pattern locked)
    - src/routes/watch/[id]/+page.svelte (D-35 metadata order — D-05 cross-link will land below the description block)
    - src/routes/work/[category]/+page.svelte (D-04 cross-link will land after the h1, before the grid)
    - scripts/test-prerender-coverage.mjs (existing threshold structure — extend with one more failures.push block)
    - .planning/phases/05-pbs-american-portrait/05-CONTEXT.md (D-02, D-03, D-04, D-05 — exact text + active-state semantics)
  </read_first>
  <behavior>
    All new assertions are RED-by-skip (describe.skip). Phase 5 Plan 05-02 flips them GREEN one-rule.

    TopNav.test.ts — 3 new assertions in a NEW `describe.skip('TopNav — Phase 5 PBS retarget + active-state extension', ...)` block:
      - (D-02) PBS category link href === '/pbs-american-portrait' (REPLACES existing href === '/work/pbs-american-portrait' — but the EXISTING test stays; the new one is in a fresh describe and gets the new contract).
      - (D-03) Active on /pbs-american-portrait/ — pbs link has text-cat-pbs class.
      - (D-03) STILL active on /work/pbs-american-portrait/ — regression that the existing surface keeps highlighting (no behavior loss).
      - (D-02 regression check) Non-PBS category nav link (e.g. Reel) still points to /work/reel — confirm the retarget is PBS-only.

    watch/[id]/page.test.ts — adds describe.skip 'PBS cross-link on /watch/[id]' covering:
      - Positive: Loading a PBS video's load() data and mounting +page.svelte renders an `<a href="/pbs-american-portrait/">` with text containing "About the PBS American Portrait project".
      - Negative: Loading a non-PBS video (use producerReelId — category 'Reel') and mounting renders NO such anchor.

    work/[category]/page.test.ts — adds describe.skip 'PBS cross-link on /work/[category]' covering:
      - Positive: Loading params.category='pbs-american-portrait' and mounting renders the cross-link anchor (href="/pbs-american-portrait/" text contains "About the PBS American Portrait project").
      - Negative: Loading params.category='reel' renders NO such anchor.

    scripts/test-prerender-coverage.mjs — extends threshold to assert `build/pbs-american-portrait/index.html` exists. Important: the script's existing failure exit (1) MUST still pass on a current build (where /pbs-american-portrait/ doesn't exist yet) — solution: scope the new check to "if the route's directory exists, the index.html must be a file"; otherwise SKIP (Plan 05-02 Task 4 wires the route + activates the check unconditionally).
  </behavior>
  <action>
    Make four additive edits. Existing assertions stay UNTOUCHED.

    --- EDIT 1: src/lib/components/TopNav.test.ts — APPEND at end of file (before final newline) ---
    Add this new describe.skip block AFTER the last existing describe block (the `TopNav — D-13 solid on non-home routes` one):
    ```ts

    // ---------------------------------------------------------------------------
    // Phase 5 D-02 + D-03: retarget PBS link to /pbs-american-portrait/ and
    // extend active-state to highlight on BOTH /pbs-american-portrait/ AND
    // /work/pbs-american-portrait/. Other 7 category links still point to /work/<slug>.
    // RED-by-skip in Plan 05-01; Plan 05-02 Task 1 turns these green.
    // ---------------------------------------------------------------------------

    describe.skip('TopNav — Phase 5 PBS retarget + active-state extension', () => {
      it('D-02: PBS link href is /pbs-american-portrait (retargeted from /work/pbs-american-portrait)', () => {
        component = mount(TopNav, { target: makeHost(), props: {} });
        const pbsLink = Array.from(host.querySelectorAll('a')).find(
          (a) => a.textContent?.trim() === 'PBS American Portrait'
        );
        expect(pbsLink?.getAttribute('href')).toBe('/pbs-american-portrait');
      });

      it('D-02 regression: non-PBS category links still point to /work/<slug>', () => {
        component = mount(TopNav, { target: makeHost(), props: {} });
        // Pick any non-PBS category — Reel is a known slug in the taxonomy.
        const reelLink = Array.from(host.querySelectorAll('a')).find(
          (a) => a.textContent?.trim() === 'Reel'
        );
        expect(reelLink?.getAttribute('href')).toBe('/work/reel');
      });

      it('D-03: on /pbs-american-portrait/, PBS link gets cat-pbs accent class (NEW surface)', () => {
        mockPage.url = new URL('http://localhost/pbs-american-portrait/');
        mockPage.route = { id: '/pbs-american-portrait' };
        component = mount(TopNav, { target: makeHost(), props: {} });
        const pbsLink = Array.from(host.querySelectorAll('a')).find(
          (a) => a.textContent?.trim() === 'PBS American Portrait'
        );
        expect(pbsLink?.className).toMatch(/text-cat-pbs/);
      });

      it('D-03 regression: on /work/pbs-american-portrait/, PBS link STILL gets cat-pbs accent class (existing surface preserved)', () => {
        mockPage.url = new URL('http://localhost/work/pbs-american-portrait/');
        mockPage.route = { id: '/work/[category]' };
        component = mount(TopNav, { target: makeHost(), props: {} });
        const pbsLink = Array.from(host.querySelectorAll('a')).find(
          (a) => a.textContent?.trim() === 'PBS American Portrait'
        );
        expect(pbsLink?.className).toMatch(/text-cat-pbs/);
      });

      it('D-03 disambiguation: on /pbs-american-portrait/, OTHER category links are NOT highlighted', () => {
        mockPage.url = new URL('http://localhost/pbs-american-portrait/');
        mockPage.route = { id: '/pbs-american-portrait' };
        component = mount(TopNav, { target: makeHost(), props: {} });
        const order = getCategoriesInDisplayOrder();
        const highlighted = Array.from(host.querySelectorAll('a'))
          .filter((a) => order.includes((a.textContent?.trim() ?? '') as (typeof order)[number]))
          .filter((a) => /text-cat-/.test(a.className))
          .map((a) => a.textContent?.trim());
        expect(highlighted).toEqual(['PBS American Portrait']);
      });
    });
    ```

    NOTE: The existing `it('category links use /work/<slug> hrefs', ...)` test inside `describe('TopNav — NAV-01 baseline rendering (D-39, D-40)', ...)` expects the PBS link to point to '/work/pbs-american-portrait'. Plan 05-02 Task 1 will UPDATE that single assertion (the PBS-specific one — line 78) to reflect the D-02 retarget. Do NOT modify it in this task — keep the existing test green against the existing TopNav code so `pnpm test` stays at exit 0 in Plan 05-01.

    --- EDIT 2: src/routes/watch/[id]/page.test.ts — APPEND at end of file (after the last existing describe) ---
    Add this new describe.skip block:
    ```ts

    // ---------------------------------------------------------------------------
    // Phase 5 D-05: PBS-only cross-link `→ About the PBS American Portrait project`
    // rendered below the description on /watch/[id]. RED-by-skip in Plan 05-01;
    // Plan 05-02 Task 3 turns this green by adding the conditional anchor.
    // ---------------------------------------------------------------------------

    import { afterEach, beforeEach, vi } from 'vitest';
    const { mockPageW } = vi.hoisted(() => ({
      mockPageW: { url: new URL('http://localhost/'), route: { id: null as string | null } },
    }));
    vi.mock('$app/state', () => ({ page: mockPageW }));
    vi.mock('$app/paths', () => ({ base: '' }));
    import { mount, unmount } from 'svelte';
    import Page from './+page.svelte';

    let hostW: HTMLElement;
    let componentW: ReturnType<typeof mount> | undefined;
    beforeEach(() => {
      mockPageW.url = new URL('http://localhost/');
      mockPageW.route = { id: '/watch/[id]' };
    });
    afterEach(() => {
      if (componentW) { unmount(componentW); componentW = undefined; }
      hostW?.remove();
    });
    function makeHostW(): HTMLElement {
      hostW = document.createElement('div');
      document.body.appendChild(hostW);
      return hostW;
    }

    describe.skip('/watch/[id] — Phase 5 D-05 PBS cross-link', () => {
      it('PBS cross-link present when video.category === "PBS American Portrait"', async () => {
        // Pick the first PBS video in the data (any will do — the link is conditional, not data-dependent).
        const pbs = videos.find((v) => v.category === 'PBS American Portrait');
        if (!pbs) throw new Error('test fixture missing: any PBS video');
        const data = await callLoad({ params: { id: pbs.id } } as Parameters<typeof load>[0]);
        componentW = mount(Page, { target: makeHostW(), props: { data } });
        const crossLink = hostW.querySelector('a[href="/pbs-american-portrait/"]');
        expect(crossLink).not.toBeNull();
        expect(crossLink?.textContent?.trim()).toContain('About the PBS American Portrait project');
      });

      it('PBS cross-link absent when video.category !== "PBS American Portrait"', async () => {
        // Producer reel — category === 'Reel'.
        const data = await callLoad({ params: { id: producerReelId } } as Parameters<typeof load>[0]);
        componentW = mount(Page, { target: makeHostW(), props: { data } });
        const crossLink = hostW.querySelector('a[href="/pbs-american-portrait/"]');
        expect(crossLink).toBeNull();
      });
    });
    ```

    --- EDIT 3: src/routes/work/[category]/page.test.ts — APPEND at end of file (after the last existing describe) ---
    Add this new describe.skip block:
    ```ts

    // ---------------------------------------------------------------------------
    // Phase 5 D-04: PBS-only cross-link `→ About the PBS American Portrait project`
    // rendered after the h1 and before the grid on /work/pbs-american-portrait/.
    // RED-by-skip in Plan 05-01; Plan 05-02 Task 3 turns this green.
    // ---------------------------------------------------------------------------

    import { afterEach, beforeEach, vi } from 'vitest';
    const { mockPageWk } = vi.hoisted(() => ({
      mockPageWk: { url: new URL('http://localhost/'), route: { id: null as string | null } },
    }));
    vi.mock('$app/state', () => ({ page: mockPageWk }));
    vi.mock('$app/paths', () => ({ base: '' }));
    import { mount, unmount } from 'svelte';
    import Page from './+page.svelte';

    let hostWk: HTMLElement;
    let componentWk: ReturnType<typeof mount> | undefined;
    beforeEach(() => {
      mockPageWk.url = new URL('http://localhost/');
      mockPageWk.route = { id: '/work/[category]' };
    });
    afterEach(() => {
      if (componentWk) { unmount(componentWk); componentWk = undefined; }
      hostWk?.remove();
    });
    function makeHostWk(): HTMLElement {
      hostWk = document.createElement('div');
      document.body.appendChild(hostWk);
      return hostWk;
    }

    describe.skip('/work/[category] — Phase 5 D-04 PBS cross-link', () => {
      it('PBS cross-link present when category === "PBS American Portrait"', async () => {
        const data = await callLoad({ params: { category: 'pbs-american-portrait' } } as Parameters<typeof load>[0]);
        componentWk = mount(Page, { target: makeHostWk(), props: { data } });
        const crossLink = hostWk.querySelector('a[href="/pbs-american-portrait/"]');
        expect(crossLink).not.toBeNull();
        expect(crossLink?.textContent?.trim()).toContain('About the PBS American Portrait project');
      });

      it('PBS cross-link absent on non-PBS category (e.g. /work/reel)', async () => {
        const data = await callLoad({ params: { category: 'reel' } } as Parameters<typeof load>[0]);
        componentWk = mount(Page, { target: makeHostWk(), props: { data } });
        const crossLink = hostWk.querySelector('a[href="/pbs-american-portrait/"]');
        expect(crossLink).toBeNull();
      });
    });
    ```

    --- EDIT 4: scripts/test-prerender-coverage.mjs — extend threshold ---
    BEFORE the final `if (failures.length > 0) {` block, ADD (preserving existing logic):
    ```js
    // Phase 5: /pbs-american-portrait/ — flat parameterless prerendered route.
    // The build emits build/pbs-american-portrait/index.html when the route exists.
    // BEHAVIOR: while Plan 05-01 stub renders the route, it's still expected to
    // prerender. Plan 05-02 builds out the full route — this check stays meaningful
    // either way: if pnpm build succeeded and adapter-static is healthy, the file
    // MUST exist.
    const pbsLandingIndex = join(BUILD, 'pbs-american-portrait', 'index.html');
    const pbsLandingExists = existsSync(pbsLandingIndex);
    if (!pbsLandingExists) {
      failures.push('Missing build/pbs-american-portrait/index.html (the PBS American Portrait landing route).');
    }
    ```
    AND in the success log block (around lines 96-103), ADD a line after the existing console.log calls:
    ```js
    console.log(`  - build/pbs-american-portrait/index.html: present`);
    ```
    AND in the FAIL diagnostic block (line 91), extend the diagnostic line:
    ```js
    console.error(
      `Found: build/work/index.html=${workIndexExists}, build/work/<slug>/index.html count=${workCategoryDirs}, build/watch/<id>/index.html count=${watchIdDirs}, build/pbs-american-portrait/index.html=${pbsLandingExists}.`,
    );
    ```

    --- IMPORTANT — Wave 0 build expectation ---
    The Plan 05-01 stub `+page.svelte` will actually cause `pnpm build` to emit `build/pbs-american-portrait/index.html` (the stub `<section><p>` renders fine through adapter-static). So `pnpm test:prerender` should pass after Task 2's stub lands. Verify both: `pnpm build && pnpm test:prerender` exits 0.

    Then re-run full validation:
    ```
    pnpm test --reporter=basic
    pnpm check
    pnpm build
    pnpm test:prerender
    ```
  </action>
  <verify>
    <automated>pnpm test --reporter=basic 2>&1 | tail -20 &amp;&amp; pnpm check 2>&amp;1 | tail -10 &amp;&amp; pnpm build 2>&amp;1 | tail -10 &amp;&amp; pnpm test:prerender</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "describe.skip" src/lib/components/TopNav.test.ts` returns at least 1 (new Phase 5 block added; existing tests untouched).
    - `grep -F "TopNav — Phase 5 PBS retarget" src/lib/components/TopNav.test.ts` returns at least 1 match.
    - `grep -F "Phase 5 D-05 PBS cross-link" src/routes/watch/[id]/page.test.ts` returns at least 1 match.
    - `grep -F "Phase 5 D-04 PBS cross-link" src/routes/work/[category]/page.test.ts` returns at least 1 match.
    - `grep -F "pbs-american-portrait" scripts/test-prerender-coverage.mjs` returns at least 2 matches (one in the failures.push block, one in the success log line).
    - `pnpm test --reporter=basic` exits 0 (every existing test continues to pass; new describe.skip blocks are skipped).
    - `pnpm check` exits 0 (no TypeScript errors in any modified test file).
    - `pnpm build` exits 0 (stub route prerenders successfully — adapter-static emits `build/pbs-american-portrait/index.html`).
    - `pnpm test:prerender` exits 0 (existing threshold checks pass + new PBS check passes because the stub route is prerendered).
    - File `build/pbs-american-portrait/index.html` exists after `pnpm build` — verify with `ls build/pbs-american-portrait/index.html`.
  </acceptance_criteria>
  <done>4 existing files extended (additive only). `pnpm test`, `pnpm check`, `pnpm build`, `pnpm test:prerender` all exit 0. Wave 0 scaffolding is complete; Plan 05-02 has every test stub it needs to flip to GREEN one-rule.</done>
</task>

</tasks>

<verification>
After all 3 tasks complete:

1. **`<approved>` paragraph captured** in this PLAN.md (Task 1 output):
   ```
   node -e "const f=require('fs').readFileSync('.planning/phases/05-pbs-american-portrait/05-01-PLAN.md','utf-8'); const m=f.match(/<approved>([\\s\\S]*?)<\\/approved>/); if(!m){process.exit(1)}; console.log(m[1].trim().slice(0,80))"
   ```
   Exits 0; prints the first 80 chars of the approved paragraph.

2. **All new files exist** (Task 2):
   ```
   ls src/routes/pbs-american-portrait/{+page.ts,+page.svelte,_pbsCollectionUrl.ts,_pbsCollectionUrl.test.ts,page.test.ts}
   ```
   All 5 files listed.

3. **All test additions present** (Task 3):
   ```
   grep -F "Phase 5 PBS retarget" src/lib/components/TopNav.test.ts
   grep -F "Phase 5 D-05 PBS cross-link" src/routes/watch/[id]/page.test.ts
   grep -F "Phase 5 D-04 PBS cross-link" src/routes/work/[category]/page.test.ts
   grep -F "pbs-american-portrait" scripts/test-prerender-coverage.mjs
   ```
   All 4 commands return at least 1 match.

4. **Full validation green:**
   ```
   pnpm test --reporter=basic   # exit 0
   pnpm check                    # exit 0
   pnpm lint                     # exit 0
   pnpm build                    # exit 0
   pnpm test:prerender           # exit 0
   ```

5. **Build artifact present:**
   ```
   ls build/pbs-american-portrait/index.html   # exists
   ```
</verification>

<success_criteria>
- [ ] User has selected a verbatim PBS copy candidate (A, B, or C) and the choice is captured under `<approved_pbs_copy>` in this PLAN.md as `<approved>...</approved>`.
- [ ] 5 new files created under `src/routes/pbs-american-portrait/` (+page.ts, +page.svelte, _pbsCollectionUrl.ts, _pbsCollectionUrl.test.ts, page.test.ts).
- [ ] 3 existing test files extended with describe.skip blocks (TopNav.test.ts, watch/[id]/page.test.ts, work/[category]/page.test.ts) — additive only, no existing tests changed.
- [ ] scripts/test-prerender-coverage.mjs extended with the `build/pbs-american-portrait/index.html` threshold (failures.push + success log line + diagnostic line).
- [ ] All 5 validation commands exit 0: `pnpm test`, `pnpm check`, `pnpm lint`, `pnpm build`, `pnpm test:prerender`.
- [ ] `build/pbs-american-portrait/index.html` exists after `pnpm build` (proves Wave 0 stub prerenders cleanly).
- [ ] PBS-02 contract is fully described by RED-by-skip stubs that Plan 05-02 will flip to GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/05-pbs-american-portrait/05-01-SUMMARY.md` capturing:
  - The chosen verbatim PBS copy candidate (A/B/C) and the first 100 chars of the approved paragraph
  - The exact line count of new test stubs added per file (so Plan 05-02 knows what to flip)
  - Confirmation that `pnpm test`, `pnpm check`, `pnpm lint`, `pnpm build`, `pnpm test:prerender` all exit 0
  - Any deviations from this PLAN (none expected — Wave 0 is mechanical)
</output>
